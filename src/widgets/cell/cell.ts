/**
 * Backwards-compatible re-export of Cell types and helpers.
 * Types are consolidated in `./types.ts` (§7.1).
 */

export type { Cell, MergeMode } from "../types.js";
export { HIDDEN_CELL, plainCell, isHidden } from "../types.js";
