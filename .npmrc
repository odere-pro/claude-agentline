# No registry auth tokens here — publish credentials are OIDC-issued in the
# release workflow only (cookbook 17, I-12).
#
# pnpm resolution/install settings moved to `pnpm-workspace.yaml` so `npm`
# (used by `agentline install --from-source` via `npm link`) does not warn
# about pnpm-only config keys it cannot interpret.
