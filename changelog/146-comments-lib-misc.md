- Lib / misc comment audit (PR-5 of the repo-wide comment sweep): two accuracy fixes plus pure multiline-block conversions across `src/lib/**`, `src/doctor/**`, `src/git/**`, `src/install/**`, `src/uninstall/**`, `src/session/**`, `src/tokens/**`, `src/theme/**`, `src/update-check/**`, `src/cli.ts`. The `src/cli.ts` top JSDoc was missing `start` and `config` from the "Top-level surface" list (both are wired in `COMMANDS` and advertised in `--help`); the `src/doctor/checks.ts` header still claimed "only D01–D04 have fixers, per spec" and referenced a `tryFix*` helper name that has never existed in `fix.ts` (the actual helpers are `fixD01`–`fixD05`, with D05 added in #138). Collapsed twenty 2–5 line `//` comment blocks into `/* … */` blocks across `src/cli.ts`, `src/doctor/checks.ts`, `src/doctor/fix.ts`, `src/doctor/fixture.ts`, `src/git/parse.ts`, `src/install/command.ts`, `src/lib/claude-project.ts`, `src/session/auth-file.ts`, `src/theme/index.ts`, `src/theme/resolve.ts`, `src/tokens/transcript.ts`, `src/uninstall/command.ts`, `src/update-check/fetch.ts`, and `src/update-check/refresh.ts`. No glossary slips were present in this subsystem.
