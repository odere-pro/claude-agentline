- Hoist the `MergeMode` string union (`"off" | "merge" | "merge-no-padding"`) into a shared `src/core/lib/merge-mode.ts` module so the data-layer's `WidgetConfig.merged` field and the widget contract's `Cell.merged` can no longer drift apart if a fourth mode is ever added; the structural overlap of the other three visual flags (`bold`, `italic`, `hidden`) is documented in `WidgetConfig` rather than enforced because boolean primitives are too small to merit a base interface.
