- Widget comment audit: fixed two stale JSDoc blocks (`src/widgets/index.ts` and `src/widgets/registry.ts` claimed built-in widgets self-register "at module-load time", but they're actually wired by an explicit `registerAllBuiltins(registry)` call) and a stale "thin space" inline comment in `src/widgets/render-widget.ts` that contradicted the adjacent `GLYPH_SEPARATOR` constant (a plain U+0020 space); collapsed four orphan 2-line `//` blocks (`time/clock.ts`, `custom/index.ts`, `custom/separator.ts`, `git/pr.ts`) into `/* … */` blocks to match the rest of the widget module.
