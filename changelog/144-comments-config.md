- Config/schema/state comment audit: dropped the retired "config layer" wording from three `src/config/` JSDoc blocks (`load.ts`, `paths.ts` both said "no per-project config layer" — the layer was removed and `docs/GLOSSARY.md` lists "config layer" under retired terms; `index.ts` advertised "Public surface of the config layer" → now "config subsystem"); replaced three "render hot path" mentions across `src/state/{stdin,version-check}-cache.ts` with the canonical "render path"; collapsed five prose `//` blocks (`render-cache.ts`, `version-check-cache.ts`, `stdin-cache.ts` × 2, `backup.ts`) into `/* … */`. No `src/schema/` comments needed changes. Comment-only diff; runtime behaviour unchanged.
