- Tidy host-contract drift surfaced by the Claude Code v2.1.193 sync: widen the `thinking-effort` widget's `Effort` union and normalise guard to recognise the new `max` level (it already printed, but future `Effort`-branching code would have missed it), and refresh `docs/cookbook/06-data-contracts.md` — add the actively-parsed `context_window` and `cost` stdin rows and list `max` for `thinkingEffort`. No per-effort colours added; the widget keeps its single session-family accent.
