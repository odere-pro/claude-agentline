- Replace the `state.pickerDraft.category as WidgetCategory` cast in `src/tui/app.ts` (the picker-widget JSX branch) with an IIFE that narrows `state.mode` to `picker-widget` and binds `category` to a `const` after a `!category` early return. TS now sees the narrowed `WidgetCategory` directly on the prop binding, so the cast and the `WidgetCategory` value import disappear. Closes smell-report 2026-05-15 `types/L-types-3`.
- Replace the `JSON.parse(raw) as Partial<NerdFontStatus>` cast in `src/lib/nerd-font.ts` (`readNerdFontStatus`) with an `isPlainObject` guard from `src/lib/object.ts`. A sentinel file containing a top-level JSON primitive previously read property `.available` off a non-object; now the function falls back to `null` (the "never detected" path the existing JSDoc already documents). Closes smell-report 2026-05-15 `types/L-types-5`.
- `test/L-test-1` read-through: confirmed the `session-usage` widget's `"block"` reset axis (`src/widgets/rate-limits/usage.ts:127`) matches the catalogue's "Percentage of the session quota consumed" description — the legacy "session" terminology refers to a Claude Code 5-hour usage block, which is exactly the `block` axis. No code change required; the labelling is consistent.
- The remaining 14 Lows in the report (code/L-1..3, arch/L-1..2, oop/L-1..2, test/L-2..3, concurrency/L-1..2, types/L-1, L-2, L-4) are explicit "no action — informational" entries: they confirm the absence of antipatterns (zero `console.log`, zero TODOs, no deep nesting, legitimate non-null assertions on bounded indices, etc.) and do not require code changes.
