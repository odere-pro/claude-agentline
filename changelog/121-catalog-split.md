- Split `BASE_CATALOG` in `src/widgets/catalog.ts` into seven per-family files under `src/widgets/catalog/`: `session.ts`, `tokens.ts`, `context.ts`, `rate-limits.ts`, `git.ts`, `time.ts`, `custom.ts`. The composition root (`src/widgets/catalog.ts`) imports each family's `<FAMILY>_CATALOG` constant and spreads them into `BASE_CATALOG`; `WIDGET_GLYPHS`, `WIDGET_CATALOG`, and the lookup helpers (`widgetMeta`, `widgetGlyph`, `widgetVariants`, `activeVariantId`) stay there. Adding a new widget to a family now means one edit in the family's file instead of grepping inside a 162-line flat object literal.
- Lift catalog types + the `entry` / `v` builders into `src/widgets/catalog/types.ts` so every family file imports from one place — and so `catalog.ts` can spread the family catalogs without a circular dependency. `src/widgets/catalog.ts` re-exports `WIDGET_CATEGORIES`, `CATEGORY_COLOR`, `WidgetCategory`, `WidgetMeta`, `WidgetMetaEntry`, and `WidgetVariant` so existing imports `from "../widgets/catalog.js"` keep resolving with no callsite changes.
- `src/widgets/catalog.ts` shrinks from 427 LOC to 166 LOC (composition + WIDGET_GLYPHS + helpers); each family file is between 16 and 79 LOC. Behaviour is unchanged: every test in `src/widgets/catalog.test.ts` passes, `gate-15-platform-matrix` (tarball install → render smoke against the canonical `model` glyph fixture) stays green.
