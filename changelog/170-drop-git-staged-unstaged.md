- Trim the git widget family: drop the redundant `git-staged` and `git-unstaged` count widgets in favour of the aggregate `git-changes` widget (which already reports staged, unstaged, and untracked counts in one cell). `git-untracked` is kept. **Breaking:** configs naming `git-staged` or `git-unstaged` render nothing and emit an `agentline: unknown widget type '…'` warning line; the shipped default does not reference either widget. The shipped catalogue drops from 36 to 34 widgets (git family 12 → 10).
