- Friendlier install experience: colourise `agentline install` output (green successes, yellow warnings, red errors, a bold-cyan brand prefix) and render file paths and URLs as underlined, OSC 8 clickable links — all gated on a TTY plus `NO_COLOR`, so piped or non-interactive output stays byte-for-byte unchanged; and add an `npm`/`pnpm` post-install greeting that introduces agentline, points at `agentline install`, links the author's GitHub/LinkedIn/Medium, and signs off with "Happy Agentic Engineering".
