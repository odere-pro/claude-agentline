- Add a "INVARIANT (load-bearing for cross-context observation)" note on `chainBackgroundRerender` in `src/tui/tui/use-save-flow.ts` explaining why the local `next` snapshot before the `.catch().finally()` is what prevents an older finalizer from clearing a newer chain reference when two background-rerender chains overlap, and confirming the SIGTERM handler's read against the same tracker is harmless under the same invariant.
