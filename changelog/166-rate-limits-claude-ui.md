- Rework the rate-limits widget family to mirror the host's usage-limits screen. **Breaking:** remove `session-usage`, `weekly-opus-usage`, and `weekly-sonnet-usage`; hard-rename `block-reset-timer` → `current-session-reset-timer`, `block-reset-at` → `current-session-reset-at`, `weekly-reset-timer` → `week-limit-timer` (no aliases — configs using the old ids must be updated; the shipped default template is migrated). Add combined all-model `current-session-usage` and `weekly-usage` widgets (`current-session-usage` replaces the old per-session `session-usage`, matching the host's "Current session" wording). Add a configurable weekly reset anchor — `options.resetWeekday` (`0`=Sunday … `6`=Saturday) and `options.resetHour` (`0`–`23`), honoured by `week-limit-timer`, `weekly-reset-at`, and `weekly-usage`, defaulting to the historical local Monday 00:00 — so the weekly countdown can track an account's real reset (e.g. Thursday noon) and shows days remaining (`3d 13h`) instead of an incorrect short countdown.
