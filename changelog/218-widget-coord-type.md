- Name the `(line, at)` widget-slot coordinate as `WidgetCoord` in `data/config/mutate.ts` and have `RemoveWidgetSpec`, `ReplaceWidgetSpec`, and `SetWidgetOptionSpec` extend it so the pair stops reading as two unrelated primitives at the call site; the optional-`at` specs (`AddWidgetSpec`, `MoveWidgetSpec`) keep their own shape because TS interface inheritance can't widen `at: number` to `at?: number`.
