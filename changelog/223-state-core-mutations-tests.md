- Add direct unit suites for `src/tui/tui/state-core.ts` (16 tests covering `clamp`, `padToMaxLines`, `replaceLine`, `replaceAt`, `isPickerMode`, `FORBIDDEN_OPTION_KEYS`) and `src/tui/tui/state-mutations.ts` (13 tests covering `moveCursor`, `moveWidget`, `deleteWidget`, `setOption`) so the pure-function primitives the reducer composes are asserted in isolation, not only as side effects of the existing `state.test.ts` reducer-level coverage.
