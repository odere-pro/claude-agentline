- Doctor `--fix` learns D05: when the host wants Powerline or `glyphs="nerd-font"` but has no Nerd Font installed, `agentline doctor --fix` now downloads JetBrainsMono Nerd Font from the pinned upstream release and extracts it into the platform's per-user font directory; idempotent on a healthy host, degrades to a `warn` with the manual-install hint on any network or extraction failure.
