- Improve install and templates: `agentline install` and `agentline uninstall` CLI subcommands added (delegate to `scripts/install.sh` and `scripts/uninstall.sh`); `install.sh` wires the local project by default and adds `--global`/`--local-only` flags for controlling global wiring; `thinking-effort` and `block-reset-timer` added to default and minimal templates; `model` widget now renders in the theme's accent colour; `agentline preview` passes a live demo token snapshot and git state to the render pipeline.
