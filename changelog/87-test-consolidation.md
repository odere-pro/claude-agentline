- Test rationalisation, pass 1 (Phase 4 PR E1): collapsed seven session per-widget tests (`account-email`, `model`, `session-id`, `session-name`, `skills`, `thinking-effort`, `version`) into the existing `session-widgets.test.ts`, and four token-axis tests (`tokens-input`, `tokens-output`, `tokens-cached`, `tokens-total`) into `tokens-widgets.test.ts`. Each per-widget file was repeating the same render contract — render a `Cell`, hide on no data, honour `options.label` + `rawValue` — and re-testing helpers the group test already covered. Unique cases were preserved by case-by-case audit before deletion: stdin-fallback paths, per-axis isolation (tokens-input doesn't accidentally include cached tokens, etc.), thinking-effort case-normalisation, account-email malformed-input branches, skills "unknown variant falls back to count", and so on.
- The consolidated `session-widgets.test.ts` now carries 62 distinct cases organised as one `describe` block per widget plus standalone helper blocks for `maskEmail` and `modelDisplayName`; `tokens-widgets.test.ts` grew from 24 to 29 cases (four explicit per-axis hide assertions, four explicit per-axis no-event assertions, three per-axis isolation assertions). Both group files inherit the same `makeCtx`/`makeSnapshot` factories the per-widget files used, so call-site shape is unchanged.
- Net delta: 107 → 96 test files (-11), 1056 → 998 tests (-58, all of which were duplicate assertions across the deleted files). All 10 gates green, 2 skipped (16 / 18 — pre-existing). No coverage of unique behaviour was lost.
