- Dedupe `agentline config widget` arg-parsing: extract `readIntFlag` and `readOptionsFlag` (plus the `__proto__` / `constructor` / `prototype` rejection list) into `src/config/widget/_args.ts`; `add`, `move`, `remove`, `replace`, and `set-option` now import the shared helpers and pass their own subcommand prefix instead of redeclaring 5 near-identical copies. The `move` / `replace` / `set-option` parsers gain the more informative two-message form (`requires an integer` for a missing slot, `must be an integer, got '<raw>'` for a non-integer value) the `add` / `remove` parsers already had; one test regex in `src/config/widget/move.test.ts` updated to match. A new focused `src/config/widget/_args.test.ts` (14 tests) covers happy paths, equals-form parsing, missing-value handling, flag-collision, and the prototype-pollution guard.
- Align `agentline config widget <sub>` dispatch with the `COMMANDS` map in `src/cli.ts`: `src/config/widget-command.ts` replaces its 7-case `switch` with a frozen `WIDGET_SUBS: Record<string, { parse, run }>` table — adding a new subcommand is now a 1-row change rather than editing a switch. A new parity test in `widget-command.test.ts` asserts the table is frozen, exposes every documented subcommand with a `parse` + `run` pair, and contains no surprise entries.
