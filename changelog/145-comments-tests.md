- Test-suite comment audit: collapsed 62 two-or-more-consecutive `//` comment blocks into `/* … */` blocks across 33 unit and integration test files (`src/**/*.test.ts` plus `tests/integration/**`, `tests/tui/**`, `tests/widgets/**`) so the test corpus matches the block-style convention applied earlier to `src/widgets/**` and `src/render/**`; no glossary or accuracy fixes were needed — the test files were already free of retired terms (`preset`, `category`, `options sheet`, `hot path`, `agentline init`, `config theme`) and stale identifier references.
