- Drop the unused `ajv-formats` runtime dependency (no source ever called `addFormats`, no schema declares a `"format"` keyword), tighten `engines.node` from `>=20` to `>=20.19` so the floor matches what the tooling actually requires (notably `knip@6.14` under `engineStrict: true`), pin `prettier` exactly at `3.8.3` for deterministic formatting across machines, and constrain the `ws` security override to `^8.20.1` so a future `ws@9.x` cannot be silently pulled in.
