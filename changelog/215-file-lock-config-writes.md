- Add a `withFileLock(path, fn)` helper (`src/core/lib/file-lock.ts`) — a cross-process advisory lockfile (O_EXCL `<path>.lock` + PID-liveness stale recovery) — and serialise both `data/config/mutate.ts:persist` (the source of every `agentline config widget …` and `agentline config refresh` write) and `core/lib/settings-refresh.ts:syncRefreshInterval` (the source of every settings.json refresh write) behind it, so two concurrent CLI invocations can no longer load-mutate-write the same file with the second writer silently discarding the first's changes.
