- **Breaking:** merge the two usage widgets into one — `current-session-usage` and `weekly-usage` are removed and replaced by `session-weekly-usage`, which renders both windows on a single cell as `52% / weekly 33%` (host `rate_limits.five_hour` + `seven_day`). It includes whichever window the host ships (session-only → `52%`, weekly-only → `weekly 33%`) and hides only when neither is present; the optional plan-name prefix is unchanged (`{ "plan": "Max" }` → `Max 52% / weekly 33%`). The percent/bar/short-bar display variants are dropped (percent only). A config still referencing `current-session-usage` or `weekly-usage` is non-fatal — that cell renders nothing; re-pick `session-weekly-usage` via `agentline edit`.
