- Editor preview overflow now flows into empty rows below: when a row's widgets exceed the terminal width, the continuation visual sub-lines spill into the next empty display rows — each adopts that row's own line number and `+ add widget` affordance — instead of stacking under the source row with a blank continuation gutter. If a row below already has its own widgets, the leftover sub-lines fall back to the previous shared-gutter wrap so they still render somewhere.
