- Refresh the root README to reflect current capabilities: corrected widget count (42 across seven families), a hero example line, highlights covering powerline cap/glyph cycling, the in-session config skills, the TUI editor (picker + options sheet + live preview), the scriptable `agentline config widget` CLI, and `agentline doctor --fix` (now installs JetBrainsMono Nerd Font); reorganised Use → Install / Configure / Uninstall / Diagnose; added a Contribute block with copy-pasteable bootstrap and iterate commands; refreshed the docs table with `architecture.md` and the new `GLOSSARY.md`.
