- Document how to run agentline locally during development: `CONTRIBUTING.md` gains a "Running agentline locally" section covering the three invocation modes (`node dist/cli.mjs`, `npm link`, `install --from-source`), how to iterate (`npx tsup --watch`, `render --fixture`, raw stdin pipe), how to isolate from the real Claude Code config via `CLAUDE_CONFIG_DIR=$(mktemp -d)`, and the test-loop commands; adds a gotcha note that `npm link` silently skips the `agentline` bin symlink (and can drop a previously-linked one) when `dist/cli.mjs` is missing, and that running `npm link` from a second checkout moves the global link. Also fixes the README docs table: "Widgets (all 37)" → "Widgets (all 39)" to match the features bullet and `docs/widgets.md`.
