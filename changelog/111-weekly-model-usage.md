- Add `weekly-sonnet-usage` and `weekly-opus-usage` widgets — siblings of `session-usage` that filter transcript events by model family (`claude-sonnet*` / `claude-opus*`) before aggregating on the `week` reset axis. Per-family Anthropic-console quotas can now be visualised separately. Both widgets reuse the existing `renderUsage` helper through a new optional `eventFilter` predicate, so display variants (`percent`, `bar`, `short-bar`), colour grading, and `options.limit`/`options.label` behaviour stay identical to `session-usage`. Reset axis is `week` × `model` and stays explicit — no mixed-axis aggregation inside the shared aggregator.
