- Apply four pattern recommendations from the tmp/ audit reports: rename loose `Colour` alias to `RawColour` in `config/types.ts` (the strict union in `theme/colours.ts` stays canonical, closes risk H1); extract `adaptStatuslinePayload` from `readStdinPayload` to name the external→internal seam at the Claude Code statusline contract; add `src/lib/result.ts` (`Ok`/`Err`/`tryAsync`) and use it at the editor save handler to surface previously-swallowed rejections (closes risk H3); add an `EditorSnapshot` memento with `lastSaved` + `revert` action so unsaved edits can be discarded back to the last-saved state; persist every live render to `state/last-render.json` and have `agentline uninstall` show the cached statusline + a "run agentline install to restore" hint before removing.
