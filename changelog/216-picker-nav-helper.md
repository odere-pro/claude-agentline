- Collapse the four near-identical picker key handlers (`handlePickerGroupKey`, `handlePickerWidgetKey`, `handlePickerSearchKey`, `handlePickerVariantKey` in `editor-input-handlers.ts`) onto a shared `handlePickerNavigation` helper for Esc / ← / ↑ / ↓ / ↵ / → and a shared `handlePickerFilterInput` helper for backspace + printable-input filter handling, dropping ~80 duplicated lines while preserving every original key binding.
