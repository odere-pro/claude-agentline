- Test rationalisation, pass 3 (Phase 4 PR E3): merged the three small `src/render/` pipeline-helper test files into a single `pipeline-helpers.test.ts`. `segment.test.ts` (2 cases), `colour-depth.test.ts` (8), and `width.test.ts` (10) all tested distinct pure helpers — `plainSegment`, `detectColourDepth`, `detectTerminalWidth`/`applyWidthMode` — and were each well under 70 lines. Combining them keeps every `describe` block intact (so the structure inside the new file mirrors the originals) while thinning the vitest file count.
- Net delta: 91 → 89 test files (-2 net: three deletes minus one new combined file), tests unchanged at 976 (every assertion preserved). All 10 gates green, 2 skipped (16 / 18 — pre-existing).
- Cumulative E-series progress: **107 → 89** test files across PRs #84 / #85 / this PR (-18, 17% reduction). Honest stopping point — further consolidation (e.g. larger `render/` tests, `config/`) carries diminishing returns and higher review cost.
