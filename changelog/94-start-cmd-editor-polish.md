- New `agentline start` command replays the last cached stdin payload through the real render pipeline so a freshly-saved config can be previewed without waiting for the next Claude Code session. Editor UX polish: lowercase `s` now saves (alongside `S` and `Ctrl+S`), the save status message points at `agentline start`, the selected widget shows its catalogue name above the preview, and the `u` update verb / `open-update` action are removed (use `r` replace instead). Glyph mode now reflects in the editor's label-mode preview so toggling `g` is visibly effective, and `DEFAULT_CONFIG.glyphs` ships as `"nerd-font"` so glyphs are on by default; doctor D10 and gate-15 platform-matrix update to match.
