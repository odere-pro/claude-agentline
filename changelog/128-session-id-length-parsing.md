- Simplify the `options.length` parse in `src/widgets/session/session-id.ts`. The previous form used `Number.isInteger(settings.options.length) && (settings.options.length ?? 0) > 0 ? settings.options.length! : DEFAULT_LENGTH`, which admitted `undefined` through the `?? 0` branch and then leaned on a non-null assertion to discard it. Rebound to `raw = settings.options.length` and a single `typeof raw === "number" && Number.isInteger(raw) && raw > 0 ? raw : DEFAULT_LENGTH` predicate — the non-null assertion goes away and the TS narrowing is explicit. No behaviour change; existing 62 `session-widgets.test.ts` cases still pass.
