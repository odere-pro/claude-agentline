- Pin every devDep to an exact version (drop carets from `@types/node`, `@types/react`, `@typescript-eslint/*`, `eslint`, `knip`, `tsup`, `typescript`, `vitest`), tighten the `ws` security override from `^8.20.1` to `8.20.1`, and add `saveExact: true` to `pnpm-workspace.yaml` so a future `pnpm add` cannot reintroduce caret drift; the manifest now matches what the lockfile (and thus CI) actually installs.
