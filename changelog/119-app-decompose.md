- Decompose the TUI editor into four focused modules: `src/tui/main.ts` shrinks from 828 LOC to 76 LOC (the entry point — project gate, config resolution, mount handoff). The Ink `App` component moves to `src/tui/app.ts` (composition shell with the JSX tree). The four picker / edit keypress handlers, their per-step transient state (`stepQuery`, `stepHighlight`), and the `useInput` registration move into a new `useEditorInput` hook in `src/tui/use-editor-input.ts`. Mount lifecycle helpers (`mountEditor`, `enterAltScreen`, `fullscreenStream`, `SaveTracker`, `resolveStartingConfig`, `resolveNerdFontAvailable`, `pruneStaleWidgets`) move to `src/tui/mount.ts`. The two-line footer projection lives in `src/tui/footer.ts`.
- Behaviour is unchanged: the JSX tree, save lifecycle, signal handling, and picker drill-down all produce the same observable output and consume the same key bindings; the split is structural so each unit fits the smell-report's <200 LOC / <50 LOC-per-function guidance instead of the prior 369-line `App` god-function the report flagged as the top code-quality risk.
