- Documentation cleanup post-Phase-5: README's widget count goes 53 → 55 (PRs #68 + #69 added `key-hints` and `git-pr`); the features list now mentions the rotating shortcut hints, the opt-in PR lookup, and the new opt-in Nerd Font glyph layer; the "Configured globally" framing replaces the stale "Two-layer config" copy that pre-dated PR #53's drop of the project layer; and the docs nav row "Widgets (all 53)" goes to "(all 55)". `docs/get-started.md`'s "Browse all 53 available widgets" follows. `tmp/PLAN-widgets-theming-editor-fixes.md`'s status table is marked complete across all five phases (PRs #65–#70 with their merge commits), the now-redundant "Open items still pending" / "What's left" sections are folded into a tighter "Resolved during execution" + "Open follow-ups" pair, and per-PR highlights for #68 / #69 / #70 are appended in the existing PR-order list. No code changes; gates 10/12 green (gate-06 trademark allowlist already covers `Claude Code REPL keyboard` from PR #68).
