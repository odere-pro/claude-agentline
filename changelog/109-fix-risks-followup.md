- Decompose the TUI editor input handler: `src/tui/main.ts` now has dedicated `handlePickerGroup` / `handlePickerWidget` / `handlePickerVariant` / `handleEdit` callbacks dispatched from a small `useInput` switch, eliminating the 149-line, 5-deep callback the smell-report flagged as critical.
- Relocate `preview-fixture.ts` and its sibling test from `src/render/` to `src/tui/`: the editor-preview module always belonged on the TUI side, and the move drops the inbound `tui/ → render/preview-fixture` edge that violated the documented layer direction. `gate-19-render-no-tui-import` keeps it that way.
- Consolidate the four per-field token widgets (`tokens-input` / `tokens-output` / `tokens-cached` / `tokens-total`) into a single `src/widgets/tokens/fields.ts` factory parametrised on the totals field, removing 87 lines of byte-identical duplication.
- Name the count and rate thresholds in `src/widgets/tokens/format.ts` (`KILO` / `TEN_KILO` / `MEGA` / `DECIMAL_RATE` / `INTEGER_RATE` / `MAX_BAR_WIDTH`), and reuse `FALLBACK_WIDTH` from `src/render/width.ts` in `src/tui/preview.ts` instead of a bare 80.
- Tighten `validateConfig` to an `asserts value is AgentlineConfig` predicate so callers no longer need the redundant `as AgentlineConfig` cast in `src/render/fixture-runner.ts`.
- Atomic Nerd-Font sentinel writes: `src/lib/nerd-font.ts` now suffixes its temp file with `randomBytes(6).toString("hex")` instead of `process.pid`, matching `src/config/atomic.ts` and removing a same-process collision risk under concurrent installs.
- Name the SIGTERM exit code arithmetic in `src/tui/main.ts` (`SIGTERM_SIGNAL = 15`, `SIGTERM_EXIT_CODE = 128 + SIGTERM_SIGNAL`).
- Pin `process.env.TZ = "UTC"` in the clock-widget default-format test and tighten the TUI editor entry tests to assert explicit `result.path` / `result.saved` / `result.skipped` values instead of the prior `toBeDefined()` shape checks.
