- Test rationalisation, pass 2 (Phase 4 PR E2): collapsed two git, three rate-limits, and one time per-widget tests into the group files they were duplicating. Six deletes; coverage of unique behaviour preserved by case-by-case audit before each removal.
- **`src/widgets/git/`** — `branch.test.ts` and `changes.test.ts` folded into `git-widgets.test.ts`. New cases added to the group: dirty-role assertions split by status source (staged / unstaged / untracked, replacing the single composite assertion); `options.label` + `rawValue` paths for both widgets (the group previously only checked text shape).
- **`src/widgets/rate-limits/`** — `reset-at.test.ts`, `timers.test.ts`, `usage.test.ts` folded into `rate-limit-widgets.test.ts`. New cases added to the group: full `block-reset-at` and `weekly-reset-at` widget coverage (the group had zero coverage of either — the wall-clock variants are distinct from the duration `*-reset-timer` ones); a `tokens-low` colour-grade assertion alongside the existing `tokens-high`; a custom-label assertion for `block-reset-timer`; a `rawValue` assertion for `weekly-reset-timer`. `duration.test.ts` (helper, 18 non-trivial cases) kept as-is.
- **`src/widgets/time/`** — `time-widgets.test.ts` deleted; `clock.test.ts` and `uptime.test.ts` were already richer than the group test. The unique `registerTimeWidgets` registry assertion was folded into `clock.test.ts`.
- Net delta: 97 → 91 test files (-6 in this PR; -17 cumulative across PR E1 + E2 from the 107 baseline), 998 → 976 tests (-22 duplicate assertions). All 10 gates green, 2 skipped (16 / 18 — pre-existing).
