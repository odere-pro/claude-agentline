- Editor preview now reflects the real statusline: the `agentline edit` preview and the widget/group/variant picker render through the same resolved `{ config, theme, env }` basis as the live render (configured theme palette and `config.families` overrides, one family-accent path, picker keeps family accent + bold), and the live render itself is corrected to match — when the terminal width is undetectable (the normal Claude Code case: piped stdout, no `COLUMNS`, no width on stdin) lines no longer wrap or get dropped against a guessed 40-column fallback, the per-line cap now counts configured lines instead of wrapped physical rows so a long line can't delete the lines after it, and `account-email`/org widgets fall back to `~/.claude.json` `oauthAccount` since modern Claude Code no longer writes `~/.claude/auth.json`.
