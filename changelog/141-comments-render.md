- Render/powerline comment audit: replaced "render hot path" with the canonical "render path" in `src/render/width.ts`; corrected the `src/render/pipeline.ts` JSDoc that claimed session/tokens/git snapshots "stay undefined here" (they're actually taken from `RenderInputs` and the live render path in `runRenderCommand` loads them); collapsed eighteen 2–5 line `//` comment blocks into `/* … */` blocks across `src/render/ansi.ts`, `src/render/fixture-command.ts`, `src/render/fixture-runner.ts`, `src/render/compose.ts`, `src/render/pipeline.ts`, and `src/powerline/transform.ts` (the lone ESLint `no-control-regex` pragma in `ansi.ts` stayed as `//` so the disable-next-line directive keeps working).
