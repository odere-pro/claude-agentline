- Validate `CLAUDE_CONFIG_DIR` at the top of `scripts/lib/common.sh` so the lifecycle scripts (`install.sh`, `uninstall.sh`, `doctor.sh`) refuse an empty, relative, traversal-laden, or system-rooted value (`/etc`, `/usr`, `/var`, `/bin`, `/sbin`, `/boot`) before any write step — a misconfigured or hostile override can no longer point a privileged installer at directories the user did not intend to touch.
