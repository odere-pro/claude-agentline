- Fix a Windows-only CI flake in the git-snapshot suite (`src/data/git/snapshot/snapshot.test.ts`). On Windows runners, `git rev-parse --is-inside-work-tree` intermittently returns a non-`true` answer for ~1s after operations on a freshly created temp repo (the filesystem state has not propagated yet), so `loadGitSnapshot` reports `available:false` and the assertion throws. Lift the existing per-test `{ retry: 2 }` mitigation to the whole `on a populated repo` describe block so every test in it absorbs the propagation delay; observed on both Node 20 and 22. No production code changes — real repositories are never created microseconds before a render tick, so the availability probe is correct as shipped.
