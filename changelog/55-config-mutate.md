- Add `src/config/mutate.ts`: pure, immutable operations over `AgentlineConfig` — `addWidget`, `removeWidget`, `replaceWidget`, `moveWidget`, `setWidgetOption`, `setTheme` — each returning a new config and validating its inputs (widget `type` must be a built-in from `WIDGET_CATALOG`; line indices are capped at `MAX_LINES` = 3 and pad with empty lines on insert; widget indices must point at a real slot; prototype-polluting option keys are rejected). Violations throw `ConfigMutationError` with a stable message. Disk wrappers (`saveAddWidget`, `saveRemoveWidget`, `saveReplaceWidget`, `saveMoveWidget`, `saveSetWidgetOption`) compose `loadConfig` → mutate → `validateConfig` → atomic write to the user config — the same materialised-config shape the TUI editor writes — so the upcoming `agentline config widget …` CLI and the editor share one code path. 32 unit tests cover success, every out-of-range case, unknown types, line-cap enforcement, immutability of inputs, and a disk round-trip.
