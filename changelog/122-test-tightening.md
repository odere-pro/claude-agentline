- Fix the self-referential loop in `tests/widgets/registry-parity.test.ts`: the "no widgets in catalog that are not in registry" case iterated `Object.keys(WIDGET_CATALOG)` and asserted each value was defined — a no-op against `Object.keys`'s own output. It now cross-checks each catalogued type against `registry.list()` so a widget that disappears from the registry actually fails the test. The companion `listMeta` case stops settling for `toBeDefined()` on `name` / `description` / `category` and asserts non-empty content + a category from `WIDGET_CATEGORIES`.
- Tighten `tests/widgets/context.test.ts`: replace the all-`toBeDefined()` shape check with identity assertions (`expect(ctx.config).toBe(DEFAULT_CONFIG)`, etc.) so a mis-wired field would actually fail. Widen the `realClock` bracket test by ±60s so loaded CI runners can't trip it on a normal clock skew without losing the contract that `realClock.now()` returns wall-clock time.
- Tighten `tests/tui/main.test.ts` "loads default config" — replace the path-echo-back-only assertion with explicit `result.path` / `result.saved === false` / `result.skipped` checks, matching the tightening already applied to the two sibling tests in PR #109.
- Add a sorted-order assertion to `src/render/pipeline.test.ts`'s `MAX_WARNING_LINES` cap test: the rendered warning lines are sorted by `buildWarningLines` so they stay stable across renders, but the prior assertion only checked the count.
- Extract a `FIXED_NOW_MS` constant in `src/widgets/rate-limits/rate-limit-widgets.test.ts` to replace the two inline `Date.parse("2026-05-01T03:00:00Z")` calls — the snapshot factory and the context factory now share one named anchor.
