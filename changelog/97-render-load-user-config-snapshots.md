- Fix every non-stdin widget hiding on the live statusline: the `agentline render` path now loads the merged user config and resolves the per-tick `session`, `tokens`, and `git` snapshots before rendering. Previously the live path went through `renderForFixture` with no config layer and no snapshot loaders, so the widget context arrived with `DEFAULT_CONFIG` (model-only) and `ctx.git` / `ctx.tokens` / `ctx.session` undefined — git widgets, token widgets, context widgets, `account-email`, and `skills` all hid even when the user had wired them up. `--fixture` and `--config` invocations stay deterministic (no snapshot loading); gate-15 and the install-matrix smoke now isolate `CLAUDE_CONFIG_DIR` so the `model`-only assertion still holds against the user's actual config.
