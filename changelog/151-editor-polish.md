- Editor polish — five fixes to the `agentline edit` TUI: line numbers in the preview gutter now start at 1; the "Pick a group" picker uses per-column `Box` widths so glyph cell-width and 1-vs-2-digit counts no longer drift the table; the picker highlight is stored per mode and `pickerDraft.family` survives `picker-back` so Esc from `picker-widget` returns to the previously highlighted family; the preview's memoised stdin/snapshot cache is now keyed on `last-stdin.json` mtime so a fresh Claude Code prompt invalidates the snapshot and the editor preview tracks the live statusline; and the transient status banner (saved → … / save failed: … / glyphs: …) clears on the next keystroke instead of lingering through subsequent navigation.
