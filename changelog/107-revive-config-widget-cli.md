- Revived the `agentline config widget <sub>` CLI: `list`, `catalog` (read-only inspection), `add`, `remove`, `move`, `replace`, `set-option` (mutations through `src/config/mutate.ts` with atomic write). The flatten in PR #83 had retired these because the TUI editor was capable enough; we're bringing them back because Claude Code skills need a scriptable surface to mutate configuration from a session (no TUI to drive interactively). The `catalog --preview` flag does not come back — the picker is the live preview surface; the CLI stays preview-free for stable, fast output.
