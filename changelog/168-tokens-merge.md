- Collapse the tokens widget family from 7 widgets to 3: merge `tokens-input` + `tokens-output` into `tokens` (renders `↓<input> ↑<output>`) and `input-speed` + `output-speed` into `token-speed` (renders `↓<input>/s ↑<output>/s`), keeping the directional convention from `git-ahead-behind`; both expose configurable `inputGlyph` / `outputGlyph` (defaults `↓` / `↑`). `tokens` keeps the `reset` axis option; `token-speed` keeps `windowSec`. `tokens-cached` is unchanged. **Breaking:** remove `tokens-total` and `total-speed`, and the merged ids replace `tokens-input` / `tokens-output` / `input-speed` / `output-speed` with no aliases — configs naming a removed id render nothing and emit an `agentline: unknown widget type '…'` warning line; the shipped default and dev templates are migrated to `tokens`.
