- Scripts/root comment audit: refreshed the step list at the top of `scripts/install.sh` (was missing the Nerd-Font probe and named the obsolete `.claude/agents/` source dir) and `scripts/uninstall.sh` (was missing the skill-cleanup step and the four-vs-five mismatch with the actual run order); removed the dangling `init.sh` reference from `scripts/lib/common.sh` (no such script ships); fixed `.markdownlint.jsonc` calling agentline a "plugin" (it is not — per CLAUDE.md) and collapsed the two leading `//` lines into a single `/* … */` block.
