- Harden the supply chain: migrate the toolchain to pnpm (corepack-pinned, default-deny install scripts, 3-day release-age cooldown), force the transitive `ws` advisory to a patched range, and implement the previously-documented-only controls — a moderate+ runtime `pnpm audit` gate, PR dependency review, CodeQL SAST, a gitleaks secret scan with per-service rules, Renovate with a release-age cooldown, and OpenSSF Scorecard.
