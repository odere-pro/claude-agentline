- Family identity on the live statusline: every widget now renders its family's inline glyph and accent colour on the rendered output, the editor preview, and the picker identically (`config.families` overrides both, ASCII-degraded on non-Unicode hosts); intentional state colours (git clean/dirty, token/context thresholds, thinking-effort) are preserved via a per-cell signal flag, an explicit `widget.fg` still wins, and structural separators stay glyph-free.
