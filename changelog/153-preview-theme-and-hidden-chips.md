- Make the editor preview agree with the live statusline on widget colour: `buildPreview` / `renderSlot` now thread the resolved `Theme` into each `previewWidget` call, so `resolveRole(theme, "tokens-low" | "warning" | …)` returns the actual hex from the loaded theme instead of `undefined`. Before this, every widget's `cell.fg` came back `undefined` and the preview silently fell back to `FAMILY_COLOR` (magenta for `33%`, blue for `high`, …) while the live render painted the real theme colour (green, yellow, …) — that's why the editor and the bottom statusline disagreed on every value-coloured widget. Also give hidden chips the dimmed family accent (account-email, self-hiding widgets) instead of leaving them bare grey, so the row reads as colourful navigation rather than a single uncoloured outlier.
