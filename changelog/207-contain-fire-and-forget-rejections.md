- Contain three fire-and-forget promise chains so a runtime surprise can't surface as Node's `unhandledRejection` after the verb has already returned: attach `.catch(() => undefined)` to the `void maybeRefresh()` calls in `agentline install` and `agentline reset` (mirroring the pattern already used in the TUI editor's post-save probe), and chain a leading `.catch` before the SIGTERM `.finally` in the editor mount lifecycle so a rejected save promise can't fire `unhandledRejection` before `process.exit` runs.
