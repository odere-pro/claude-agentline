- Dedupe the picker key handlers' up/down-arrow navigation: all three handlers in `src/tui/main.ts` (and both branches of `handlePickerGroup`) now call `clampIndex(curr, length)` instead of inlining their own `Math.min(Math.max(0, len - 1), h + 1)` clamp. `clampIndex` already lived in `src/tui/picker.ts` powering `selectedAt`; it's now exported so the picker key handlers and the selection helper share one implementation.
- Refactor `parseRenderArgs` in `src/render/fixture-command.ts`: extract a `matchFlag(arg, rest, i, name)` helper that handles both `--name value` and `--name=value` forms, plus `requirePathValue` / `requirePositiveInt` validators. The previous 67-line function dropped to ~50 LOC with the four flags (`--fixture`, `--config`, `--frozen-clock`, `--width`) each handled in a uniform 5-line block.
