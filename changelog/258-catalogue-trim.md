- **BREAKING (config):** trim the widget catalogue from 30 to 22. Remove `claude-doctor`, `claude-update` (session family), `context-bar`, `context-length` (context family), `git-sha`, `git-untracked` (git family), and fold `current-session-reset-at` + `weekly-reset-at` into wall-clock format variants (`at-24h`, `at-12h`, `at-seconds` on `current-session-reset-timer`; `at-day-time`, `at-24h`, `at-12h` on `week-limit-timer`) so the timer widgets serve both countdown and absolute-time display (rate-limits family shrinks from 5 to 3). A config naming a removed type now renders a hidden cell, not an error. Update the shipped default config (line 2 `context-bar` → `token-speed`), README, docs, and the GLOSSARY counts and type tables; regenerate golden fixtures.
