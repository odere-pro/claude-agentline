- Show the host's live usage percentage in the rate-limits widgets: `current-session-usage` and `weekly-usage` now read the host's `rate_limits` block (`five_hour` / `seven_day`) straight off stdin so the value matches the host's `/usage` screen, default to a compact `21%` (with `bar` / `short-bar` variants and an optional plan-name prefix), and hide when the host omits the data instead of falling back to a transcript-derived token count; the stdin contract now models `rate_limits`.
