- Harden `command` widget and bound render-path file reads: project-layer `command` widgets are dropped unless `AGENTLINE_TRUST_PROJECT_COMMAND_WIDGETS=1`; `options.shell`, `options.cwd`, and the forwarded subprocess environment are now allowlisted (credential-shaped CLAUDE\_\* keys are stripped); transcript and auth-file reads are size-capped (16 MB / 64 KB) and the transcript path is confined to `~/.claude` (override via `AGENTLINE_TRANSCRIPT_ROOT` for tests); `themes --show <name>` rejects path-shaped names; `__proto__` / `constructor` / `prototype` keys are stripped from env-layer JSON values and from `--config` fixtures.
