- Fix stdin adapter to match Claude Code's real statusline contract so saved configs actually render: read `model.id` / `model.display_name` from the nested model object (the widget now prefers `display_name`, so variants like `claude-opus-4-7[1m]` show as "Opus 4.7 (1M context)"); read snake_case `session_id` / `session_name` / `transcript_path` / `vim_mode`; extract `effort.level` for thinking-effort and `output_style.name` for output style; fall back to `workspace.current_dir` for `cwd`. Previously only `cwd` and `version` parsed correctly, so model / thinking-effort / output-style / session widgets all hid on a real Claude Code session even when the saved config requested them. Also reword the editor's saved-confirmation status so it no longer suggests a Claude Code restart — the next prompt picks up new config automatically.
