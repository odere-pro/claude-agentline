- `agentline config` now shows a live preview of the statusline. A bordered region at the top of the editor renders the current line list against the synthetic demo session via the real render pipeline (`previewStatusline` from `src/render/demo-fixture.ts`), so what you see is what the bar would look like — and it updates as you edit, since the editor recomputes it from the reducer's `lines` on every change. New `src/tui/preview.ts` (`Preview` Ink component + a pure `previewLines` helper); the TUI bundle (`dist/tui.mjs`, lazily loaded only when `agentline config` runs) grows to pull in the render pipeline, leaving the render hot path untouched. 6 new tests.
