- Add unit tests for the three previously-untested branchy modules — `parseRefreshArgs` (`config refresh` CLI argv parsing), `syncRefreshInterval` (host settings.json write contract across all five return kinds), and the picker drill-down/commit transitions in `state-picker.ts` — closing the test-coverage gaps the risk report called out (test-H5, H6, H8) without touching the existing locale-tolerant rate-limit assertions.
