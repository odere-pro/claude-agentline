- Editor picker hides widgets already placed in the layout (and drops groups whose every widget is in use), paints each group with its own accent colour (session/blue, tokens/yellow, context/magenta, rate-limits/red, git/green, time/cyan, custom/white), and `agentline install` now probes the host for a Nerd Font: when none is detected the result is written to a `nerd-font.json` sentinel and the TUI editor clamps `glyphs` to `"off"` and refuses the `g` toggle so the rendered statusline can't ship tofu boxes.
