- Make `agentline doctor` coherent with the shipped code: retire the D06 pricing-table-freshness check — no widget consumes the pricing table at runtime, so freshness moved to a unit test plus the new `gate-22-pricing-freshness` repo gate; rework the config-directory check to always probe the resolved global dir `${CLAUDE_CONFIG_DIR:-~/.config}/agentline` (treating a not-yet-created directory with a writable parent as healthy) instead of skipping when `CLAUDE_CONFIG_DIR` is unset; fix the themes check to honour `CLAUDE_CONFIG_DIR` instead of a hardcoded path; and renumber the remaining checks to a contiguous `D01–D08`.
