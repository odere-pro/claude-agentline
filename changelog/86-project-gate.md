- New `src/lib/claude-project.ts`: `isClaudeProject(cwd)` is a pure async filesystem probe — `cwd` counts as a Claude project when it contains either a `.claude/` directory or a `CLAUDE.md` file. `projectGate({ command, cwd?, stdin?, stderr? })` is the shared pre-flight gate: inside a Claude project it returns `"proceed"` straight away; outside one it prompts `[y/N]` on a TTY (default `N`) and returns `"skip"` silently when stdin is not a TTY so scripted pipelines don't get accidentally wired up. `render` never calls it — the hot path that Claude Code re-invokes on every prompt has to stay decisive.
- Wired the gate into `runInitCommand`, `runInstallCommand`, and `runConfigCommand` (the `edit` entry-point). Each now accepts `cwd?` and `stdin?` overrides for tests; in production the defaults (`process.cwd()`, `process.stdin`) carry the gate's behaviour transparently. `runConfigCommand` returns a new `skipped?: boolean` so the CLI can distinguish a gate skip from a normal exit. Existing tests run from the repo root (itself a Claude project) and pass the gate without modification; new tests in each command's test file exercise the non-TTY skip path with a `PassThrough` stdin.
- Rewrote `agents/agentline-onboarding.md` to match the flat command surface from Phase 1 (`edit`, `init`, `keys`) and document the pull-based reload model: a config change does not take effect mid-prompt; it applies the next time Claude Code re-invokes the bin. New section on what `install` actually did (five reversible steps tracked in the manifest), a clearer story on the editor's live preview (real values from cached stdin, falls back to widget type names when the cache is empty), and an updated quick-reference table. Retired references to dropped commands (`agentline config theme`, `--preset`) and the retired `flex-separator` widget.
- Extended the `gate-06-trademark` allowlist with five more contexts that surface in the new module's prose and the rewritten onboarding skill: `Claude project`, `Claude-project`, `send Claude a`, `Claude Code invokes`, `inside Claude Code sessions`. Each is a factual reference, not endorsement copy.
- Verification: 1069 unit tests passing (across 108 files; +13 from `claude-project.test.ts` and the three new wiring tests), 10 gates green, 2 skipped (16 / 18 — pre-existing).
