- Make the agent-operable surface drift-resistant: rewrite `SOFTWARE-3-0.md` into a seven-surface, parity-checked map (dual on-ramp, compressed thesis, context-planner, trimmed worked example); ship gate-12 (render determinism over the published bin) and gate-27 (citation existence across the docs); extend gate-22 to derive the surface-map counts from code so no hardcoded count can rot; pin the one-syscall write; add the agentic-engineering glossary rows; and reconcile the verb set, the `--json` envelope, and the render-cache contract across the doc set.
