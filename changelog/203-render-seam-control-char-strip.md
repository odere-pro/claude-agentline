- Strip C0 / DEL / C1 control characters from widget cell text at the cell→segment boundary so an attacker-controlled `model.display_name`, git branch, or PR title can no longer inject ANSI escapes (screen clear, window-title spoof, cursor move) into the user's terminal on every render tick; extend the OSC 8 href sanitiser to drop C1 bytes for the same reason; and route the TUI editor's typed-input sanitiser through the same shared helper so widget text and typed input cannot diverge on what counts as a printable byte.
