- Tidy the root README: drop the redundant FAQ section, reconcile drift (stale widget count, `update-check`-as-verb, and the CLI surface list), and add an npm version badge linking to the package; correct the `package.json` description to 30 widgets and drop the theming claim; bring the GitHub Pages landing page up to date (widget count 28 → 30 with the two missing session widget cards, `softwareVersion` 0.1.4 → 0.3.0, and theming dropped from the description meta tags); replace the README License section with a centered footer linking the landing page and the MIT licence (allowlisting `p`/`a` in the markdownlint config).
