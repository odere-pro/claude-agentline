- Editor preview wraps overflowing widgets to a new visual row instead of squeezing a single cell across two lines: each preview slot now lives in a `flexShrink: 0` Box inside a `flexWrap: "wrap"` row, so when a widget doesn't fit on the current preview line it drops to the next one whole — `worktree-shiny-forging-sutton` no longer renders as `worktree-shiny-forging-su` + `ton`. README drift fix in the same pass: the "CLI surface" sentence advertises the actual verbs (`install` · `uninstall` · `doctor` · `edit` · `start`) instead of the long-gone `config` subcommand, the get-started skill paragraph points at `agentline edit` instead of the removed `agentline config widget …` subcommands, and the docs nav row bumps "Widgets (all 37)" to "(all 39)" to match the catalogue.
