- Redesign the shipped default preset: a three-line layout ordered identity → capacity → budget (model · thinking-effort · git-branch · git-changes / context-percentage · context-bar · tokens / session-weekly-usage · current-session-reset-timer · week-limit-timer), dropping `account-email` so no personal data ships by default; the template round-trip test and every doc that names the default (cookbook feature catalogue, config, install, get-started, widgets) updated to match, and the stale one-line/`minimal.config.json` claims removed.
