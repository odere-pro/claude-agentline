- Extract the consumer-visible type surface of `src/data/config/mutate.ts` (specs, `ConfigMutationError`, `WidgetCoord`, `SaveOptions`, `MAX_LINES`) into a new `mutate-types.ts` module so a caller can import the vocabulary without pulling in the `loadConfig` / `writeJsonIdempotent` / `withFileLock` machinery the pure ops never touch; `mutate.ts` re-exports everything so existing `import { … } from "./mutate.js"` call sites stay valid byte-for-byte.
