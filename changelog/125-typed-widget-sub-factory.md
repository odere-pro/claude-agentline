- Strip the per-entry `as WidgetSub<unknown>` cast from `src/config/widget-command.ts:WIDGET_SUBS`. A new `defineWidgetSub<TArgs>(parse, run): WidgetSub<unknown>` factory captures the args-shape linkage at the call site (TS sees both halves agree on `TArgs`) and confines the type erasure to one place. Mismatched pairs — passing `parseWidgetRemoveArgs` with `runWidgetAddCommand` — now fail to type-check; previously they compiled. A `// @ts-expect-error` block in `widget-command.test.ts` pins this guarantee.
- Strip the per-entry `as WidgetDef<unknown>` cast from the six widget family `index.ts` files (`rate-limits`, `context`, `git`, `tokens`, `time`, `custom`). A new `eraseWidget<T>(def: WidgetDef<T>): WidgetDef<unknown>` helper colocated with `defineWidget` in `src/widgets/types.ts` (and re-exported from `src/widgets/widget.js`) carries the single contravariant cast; each family array now reads `eraseWidget(sessionUsageWidget)` rather than `sessionUsageWidget as WidgetDef<unknown>`. 36 inline casts replaced.
- The cast is sound in practice because the registry only ever calls a widget's `render()` with settings whose `options` shape matches the widget's expected `TOptions` (the schema validates before dispatch). The helper's doc comment spells this out.
