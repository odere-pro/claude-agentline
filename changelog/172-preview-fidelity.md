- Make the editor preview faithfully match the live statusline: the `context-length` widget now reports the current context-window occupancy via the same resolver as `context-percentage` (e.g. `180k / 1M`) instead of a cumulative session sum that ballooned past the window, so the figure agrees with the host's own "N% used" and is usable for deciding when to `/clear` or `/compact`; and preview chip colours are resolved through the render bin's own colour model at the colour depth the bin would detect for the terminal — named accents are no longer washed out through the terminal palette, the `colour:NNN` indexed form is no longer dropped, and the editor pins chalk to truecolor so the pre-resolved swatch is not re-downsampled.
