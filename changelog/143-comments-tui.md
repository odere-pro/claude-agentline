- TUI/keys comment audit: three "render hot path" mentions (`src/tui/app.ts`, `src/tui/preview-fixture.ts` × 2) became the canonical "render path"; the stale "options sheet" reference in `src/tui/editor-input-handlers.ts` (the surface was removed in the editor sweep) got rewritten to describe what `a`/`r` actually do today; collapsed 25 prose `//` blocks across `src/tui/` (app, editor-input-handlers, state, state-picker, use-editor-input, mount, picker, preview, preview-model, main, glyphs, footer) into `/* … */` blocks, leaving the box-drawing section-divider banners in `picker.ts` as `//` on purpose; `src/keys/` was clean. No runtime behaviour changes.
