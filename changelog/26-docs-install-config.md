- Document the consumer surface: `docs/install.md`, `docs/config.md`, `docs/widgets.md`, `docs/themes.md`, `docs/keymap.md`, and `docs/doctor.md` cover the install + manual recipes, the layered config and JSON Schema, the built-in widget catalogue with reset axes, the four shipped themes plus Powerline, the TUI editor keymap and overrides, and the full D01–D10 doctor checks with exit codes.
