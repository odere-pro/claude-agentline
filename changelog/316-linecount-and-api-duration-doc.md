- fix: `agentline render`'s diagnostic render-cache reported `lineCount` one too high. The rendered frame ends with a single trailing newline, so splitting on newlines counted a phantom empty segment and a three-row statusline logged `lineCount: 4`. The figure is diagnostic only — it backs the "last statusline" view in `agentline uninstall` — but an operator debugging a row-count problem would have trusted it. Extracted as a tested `countRenderedLines` helper. Also documents that `api-duration`'s `percent` variant shares the idle-inclusive `cost.total_duration_ms` denominator that got `cost-efficiency` retired; it is kept because, unlike that widget, its name describes exactly what it measures.
