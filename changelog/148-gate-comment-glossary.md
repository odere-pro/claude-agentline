- `tests/gates/gate-21-comment-glossary.sh` — new gate that enforces the `docs/GLOSSARY.md` "Retired terms" table on TypeScript source comments under `src/**/*.ts`. Mirrors the gate-20 idiom (`check_in_comments <pattern> <label>` helper, comment-line prefix anchor `^[[:space:]]*(//|/\*|\*)`) and reports each hit as `file:line`. Picked up automatically by `tests/gates/run-all.sh`. Also drops the stale "TypeScript code may still spell this `WidgetCategory`" exemption note from gate-20 — the rename to `family` is complete and gate-21 now covers the TS surface gate-20 deliberately leaves alone.
