- Add `agentline reset` as the user/agent-facing entry point and hide `install` from the CLI surface. `reset` delegates to `scripts/install.sh --reset`: it overwrites the user config with the shipped default template (the bare installer preserves an existing config — exactly the confusion this removes), re-seeds themes and skills, and ensures `statusLine` is wired, including first-time wiring on a host that was never set up. `install` is no longer listed in `agentline --help` but is still dispatched, so npm/postinstall, the `--from-source` dev flow, and existing scripts keep working. Only `config.json` is overwritten by `reset`; edited themes/skills and the first-writer-wins pre-install `statusLine` backup are preserved. The first-run hint and the doctor / troubleshoot / onboarding / configure guidance now point at `agentline reset`.
