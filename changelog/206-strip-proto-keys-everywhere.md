- Centralise the recursive `__proto__`/`constructor`/`prototype` strip into a single `src/core/lib/strip-prototype-keys.ts` helper and apply it at the two JSON-parse seams that were previously asymmetric — user `config.json` loader (`src/data/config/load.ts`) and the theme loader (`src/data/theme/index.ts`) — so a hostile payload nested under the `widgets[].options` or `palette` `additionalProperties: true` carve-outs cannot pollute `Object.prototype` even if a downstream consumer ever bypasses the merge layer's existing skip.
