- README post-#140 cleanup: the README refresh landed wording that tripped `gate-02-no-absolute-paths` (literal `~/.claude/settings.json` and `~/.claude/agents/` on line 50) and `gate-06-trademark` (three "Claude Code" contexts on lines 11, 24, 122 that were missing from the allowlist); rephrased each to use already-allowlisted phrases (`Claude Code's statusline contract`, `wired into Claude Code`, plain `to Claude Code`) and dropped the literal paths in favour of `$CLAUDE_CONFIG_DIR`-relative wording; also retired the stale "options sheet" mention on line 25 (the options sheet was removed in the editor sweep).
