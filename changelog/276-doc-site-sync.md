- Sync the documentation surfaces to the 38-widget catalogue (the breaking 6-family regrouping below lands in the same change):
  - **GitHub Pages (`site/index.html`)** — updated every stale "25 widgets" count to 38 (meta description, JSON-LD FAQ, hero lead + pills, the features section, and the widget-catalogue heading/filter), corrected the per-family filter counts (session 9 → 17, tokens 4 → 8, context 1 → 2), and added cards for the 13 widgets shipped since the 25-sync — `cwd-path`, `clock`, `output-style`, `vim-mode`, `agent-name`, `project-dir`, `added-dirs`, `thinking-enabled` (session); `context-200k-flag` (context); `cost-burn-rate`, `api-duration`, `cost-efficiency`, `cost-vs-limit` (tokens). Each card's copy is taken from the catalogue/glossary. The FAQ coverage prose (both JSON-LD and visible) now mentions cost burn rate, budget-vs-limit spend, API duration, the 200k-context flag, and the working-directory/clock/output-style/vim-mode/agent-name session widgets. `sitemap.xml` lastmod bumped.
  - **Root `README.md`** — expanded the configure-paths table to the full scriptable config surface: `config widget` now lists `replace`, plus rows for `config init`, `config refresh`, and the `config undo` / `redo` rollback pair.
  - **`CLAUDE.md` files** — refreshed `src/data/CLAUDE.md` and `src/data/config/CLAUDE.md` to describe the config writers accurately (the `widget`/`init`/`refresh` verbs plus the backup-backed `undo`/`redo` 2-slot rollback and the `backup/` write seam). Widget counts everywhere stay catalogue-derived (gate-20/22); no count was hand-edited.
- **feat(widgets)!: four breaking widget/family changes — catalogue stays 38, now reorganised into 6 families.** Existing configs naming a removed type render the unknown-type path; intentional, no migration shim.
  - **Merged reset timers.** `current-session-reset-timer` + `week-limit-timer` are **removed** and replaced by a single **`reset-timer`** that renders BOTH windows on one cell — countdown (`reset in 1h 30m · weekly 3d 4h 0m`) or wall-clock (`resets 18:30 · weekly Mon 17 00:00`). One `format` variant drives both windows (`short`/`long`/`clock` + `at-24h`/`at-12h`/`at-seconds`); no reset axis (host scalars). The `default` and `power` templates swap the two timer entries for one `reset-timer`.
  - **New 6th family `other`** (cyan, glyph `○` / `[o]`), with **`clock`, `added-dirs`, `output-style`, `vim-mode`** moved into it (catalogue `family` field only — render code stays under `src/widgets/session/`).
  - **`project` + `project-dir` moved to the `git` family** (git 8 → 10; the project name is git-repo-derived).
  - **Context cached, two ways:** a `showCached` option on `context-percentage` (off by default, so goldens are unchanged: `37% · 0.8k cached · 200k`) **and** a new standalone **`context-cached`** widget, both reading the same session cached-token source.
  - Final per-family counts: **session 11 · tokens 8 · context 3 · rate-limits 2 · git 10 · other 4.** The `widget.type` schema enum and the family identity / schema / `en-dictionary` / gate-22 contracts were extended for the 6th family.
