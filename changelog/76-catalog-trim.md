- Trim the built-in widget catalogue from 55 entries down to 37 — drop widgets that did not earn their slot in the picker or the docs. **Session**: `login-method`, `org`, `output-style`, `vim-mode`. **Tokens**: `cost` (the embedded pricing table stays in the binary for future cost-aware widgets, just no surfaced widget). **Rate-limits**: `weekly-usage`, `block-timer`, `model-usage`, `effort-usage`, `compaction-counter` — leaving the three reset-themed entries (`session-usage`, `block-reset-timer`, `weekly-reset-timer`). **Git**: `git-status`, `git-insertions`, `git-deletions`, `git-origin-owner`, `git-is-fork`. **Custom**: `command`, `flex-separator`, `key-hints` — the family collapses to `separator` only. Every widget source file (and its `.test.ts`) was deleted; `BASE_CATALOG`, `WIDGET_GLYPHS`, and the family `index.ts` barrels in `src/widgets/{session,tokens,rate-limits,git,custom}/` were trimmed; widget-family roll-up tests (`session-widgets.test.ts`, `tokens-widgets.test.ts`, `git-widgets.test.ts`, `rate-limit-widgets.test.ts`, the merged `separator.test.ts` replacing `custom-widgets.test.ts`) were rewritten to match the new registered set. Call sites scrubbed: shipped templates (`templates/{default,maximal}.config.json`), doctor (D09 / `command` widget check removed), preview-model and options-sheet tests (test fixtures retargeted to remaining widgets — `git-conflicts` now stands in for the "self-hiding widget" preview probe), config widget catalog test (the 55 count drops to 37), config widget add / replace / mutate tests (cost → session-usage / tokens-total as the representative widget), `formatCost` dropped from `tokens/format.ts` and its sibling tests since it had no remaining production caller, and the `cost.low/mid/high` theme role row was dropped from `docs/themes.md`. Spec + docs sync: `docs/widgets.md` widget tables, `docs/plan/SPEC-v0.1.0.md` §7.2–§7.8 widget tables and §F1/F3/F6/F7/§7.10 prose, `docs/doctor.md` (D09 removed), `docs/cli.md` (D09 and preset bullets), `docs/config.md` (preset bullets, layout-only widgets list), `docs/get-started.md` (preset summary + skill prompt example), `docs/troubleshooting.md` (cost / command-widget sections removed), `README.md` (widget count). `npm test` is 1121 passing across 112 files; `bash tests/gates/run-all.sh` reports 10 / 12 with the two skips matching `main`. Goldens unchanged because none of the dropped widgets appear in the golden fixtures.
