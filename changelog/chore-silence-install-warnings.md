- Silence install-time noise: `pnpm install` and `agentline install --from-source` no longer print pnpm's "Ignored build scripts: esbuild" nag or npm's "Unknown project config" warnings — pnpm-only settings moved from `.npmrc` to `pnpm-workspace.yaml` (invisible to the `npm link` step) and `esbuild`'s deliberately-skipped build script is recorded under `ignoredBuiltDependencies` so the script still never runs.
