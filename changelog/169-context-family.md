- Rework the context widget family. `context-bar` now renders in the `context` family accent (`config.families.context.colour`) instead of self-colouring with the green→yellow→red usage thresholds, so an empty bar no longer reads as a solid alarming block and the bar matches the family everywhere it appears. **Breaking:** remove `context-percentage-usable` (the `0.8 × window` "usable" variant) with no alias — configs naming it render nothing and emit an `agentline: unknown widget type '…'` warning line; the shipped default uses `context-percentage` and is unaffected. All three remaining context widgets (`context-length`, `context-percentage`, `context-bar`) now append the current model's context-window size as an always-on postfix — `45.2k / 200k`, `37% · 200k`, `████░░░░ 200k` (`1M` for the 1M-token model variants); the postfix is omitted when only the synthetic fallback window is available.
