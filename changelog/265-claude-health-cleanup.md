- Clean up the `claude-health` subsystem orphaned by the catalogue trim. The `claude-doctor`/`claude-update` widgets it fed were removed, leaving the render hot path carrying a `claudeHealth` context field no widget reads and spawning a detached `claude` probe on every stale render just to warm a cache whose only remaining reader is doctor check D10.
  - **D10 now owns its data**: `agentline doctor` refreshes the claude-health cache itself (a best-effort, lazily-imported inline probe) when it runs. Its missing/unpopulated-cache → `pass`-with-explanation fallback is unchanged.
  - **The render path no longer spawns a probe** — removed the `claudeHealth` field from the widget/render context, the per-render `maybeSpawnClaudeHealthRefresh` call, and the hidden `__refresh-claude-health` verb. One fewer process fork on the hot path; no golden changes (the field was never rendered).
  - The probe + cache machinery and the `D10` check (and its `cmd.doctor.d10.*` strings) are retained; no doctor-check renumbering.
