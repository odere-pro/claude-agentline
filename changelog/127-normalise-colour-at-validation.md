- Tighten the colour fields on `WidgetConfig` and `GlobalConfig` from `RawColour | null` (a loose `string` alias) to the strict `Colour | null` union (named / `colour:N` / `#RRGGBB`) in `src/config/types.ts`. The schema's `colourOrNull` pattern was already enforcing the same set at runtime — `validateConfig` now does a belt-and-braces re-check with `isColour` after AJV so a future schema-pattern drift can't silently leak unvalidated strings into a typed `AgentlineConfig`. The render path therefore consumes typed colours and the boundary casts disappear.
- Drop the four `config.fg as Colour` / `config.bg as Colour` casts in `src/widgets/render-widget.ts` (both `applyOverrides` and `renderWidgetLabel`), plus the now-unused `Colour` import and the explanatory boundary-cast comment.
- Drop the two `as string` casts in `src/tui/preview-model.ts` — `widget.bg` is now `Colour | null | undefined` and `cell.bg` is `Colour | undefined`, both already assignable to the `PreviewSlot.fg`/`.bg` string fields.
- Drop the redundant `parsed as ThemeJsonShape` cast in `src/theme/index.ts`. The `ValidateFunction<ThemeJsonShape>` type predicate narrows `parsed` already after the `!validate(parsed)` guard; the comment now explains why the per-role `isColour` re-check remains as defence against schema drift.
- `RawColour` stays exported from `src/config/index.ts` (now `@deprecated`) so external consumers of the config-layer barrel keep resolving; new code points at `Colour` from `../theme/colours.ts`.
